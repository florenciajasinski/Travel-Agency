
    <label for="{{ $id }}" class="text-gray-700 font-semibold mb-1">{{ $label }}</label>
    <select id="{{ $id }}" class="border px-3 py-2 rounded text-sm w-full">
        <option value="">Select {{ $label }}</option>
    </select>

